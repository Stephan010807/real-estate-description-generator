import Layout from '@/components/Layout';
import FormContainer, { EnhancedPropertyForm } from '@/components/FormContainer';

export default function Home() {
  return (
    <Layout>
      <EnhancedPropertyForm />
    </Layout>
  );
}
